# LucentMist 0.9.7 发布说明

状态：已推送且 CI/GHCR/health 通过，但发布镜像真实对话发现两项事实矛盾，**不能判定最终语义验收通过**。修复将以0.9.8发布；此标签不改写。见[完整产物证据](release-artifact-validation-0.9.7.md)。

## 为什么升级

0.9.7 标识 0.9.6 后的网关分析修复，不覆盖旧标签。升级前备份扫描数据；回滚可使用保留的 0.9.6 镜像及其发布说明中的摘要。

- 完成度检查允许有限但有用的安全评估；未知版本或一次失败不再要求无限补查。
- HTTP HEAD/GET 探测区分未公开版本与抓取失败；DNS 在同次 Agent 分析共享快照，不把多次请求当成同一观察。
- 老且无版本证据的云端 CVE 线索折叠；默认最多5条，原始工具数据保留，`-v` 查看详细过程。
- 端口断言按目标合并实际受检范围，识别任意1–65535数字端口及共享服务目录，不再只硬编码RDP/SSH等四项。未扫端口不等于未开放。
- DNS 单次响应比不作为低风险阈值；不因数值是2.1、0.1或20就声称安全。
- 精简模型结论后的工具事实补充，仍保留TLS信任错误、过期、来源和覆盖边界。

## 真实复现证据

发布前的[四轮真实 DeepSeek 记录](gateway-deepseek-reproduction-20260831.md)保留完整失败→修复→复验过程；不是本次GHCR产物验证的替代品。
已使用GHCR不可变digest执行真实DeepSeek：网关3轮、容器回环3轮，契约6/6；网关完成53/80/443、DNS/TLS及线索折叠。但模型把2031年到期说成2026年，又把范围内22端口列为未检查。契约成功不等于事实正确，这次语义验收未通过。

## 已知限制与部署

- 网关版本未知时只能给暴露面、TLS风险、线索与下一步，不是确认CVE，也不能宣称整体安全。
- 云源可能404/超时/限流；协议关键词搜索不是产品或版本匹配。
- DNS结论只适用于本次扫描源。容器内127.0.0.1是容器自身，不是Windows宿主机；容器网卡与宿主机网卡也不是同一视角。
- 断言守卫是带预算的防误导机制，不是对所有自然语言变体的形式化证明。原始扫描范围/错误/来源仍是证据。
- 生产/非回环部署必须设置强API和Web凭据，外部模型key只放受保护环境；本次验证使用隐藏stdin传入进程环境，不放Docker配置或命令参数。
- 扫描及报告含敏感网络数据，SQLite明文；按自用手册限制权限并备份。只扫描明确授权目标。

## 发布门禁

本地 Release build：0警告0错误；578个.NET测试（Tools392、Agent127、Scanning31、API28），0失败0跳过；14个Python测试通过。format、Compose、diff检查通过。
main/tag CI三job成功；manifest `sha256:9eb19f0ea1c199af9e5f5127b3aebb27b5206c7b63afc7bced2b3222f0f2e0d3`；独立health返回0.9.7。两次产物对话后工作树/暂存区/全部Git对象的精确密钥检查ABSENT。语义门禁失败如上，不建议将本版视为本轮最终交付。
