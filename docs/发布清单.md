# LucentMist 0.9.6 发布清单

> 日期：2026-08-31。状态：发布进行中。未完成项不能用历史证据代替。

## 本地门禁

- [x] 原始待推送代码：Release build 0 警告/0 错误；533 个 .NET + 14 个 Python 测试通过
- [x] 原始代码 format、Compose、diff 检查通过
- [x] 精确授权 key 检查：工作树/暂存区/全部本地 Git 对象 ABSENT
- [x] 0.9.6 版本引用更新；旧发布及验证文档保留历史版本
- [x] 版本化后的完整 build/test/format/Compose 门禁通过
- [ ] 发布提交与 v0.9.6 标签已推送

## 远端实证

- [ ] 本次 main push CI 的 Windows、Ubuntu、GHCR 三 job success
- [ ] v0.9.6 tag CI 的 Windows、Ubuntu、GHCR 三 job success
- [ ] docker manifest inspect 可读 0.9.6、main/latest，旧 0.9.5 仍可读
- [ ] 版本镜像拉取、摘要比较和 health 200 独立验证通过
- [ ] [final-push-evidence](final-push-evidence.md) 保存真实命令输出与 Actions 链接

## 用户交付

- [x] [发布说明](RELEASE_0.9.6.md) 记录五类真实修复、四轮 77 次调用及最终 17/17
- [x] [自用手册](SELF_USE_GUIDE.md) 说明 loopback、Docker 端口限定、凭据、报告、审计和备份恢复
- [x] 网关线索非确认漏洞；Agent 保持实验性；未检查不等于安全
- [x] 原有未提交锁文件依赖变更与未跟踪目录不混入发布

生产/非回环部署必须设置强 API token 与 Web 凭据。Docker 自动生成的随机值留在受限权限数据卷文件，
日志只打印位置，不打印值。升级前先备份；历史版本的精确回滚点见对应发布说明，不改写旧 tag。

```text
dotnet build -c Release
dotnet test -c Release --no-build
dotnet format --verify-no-changes
python -B -m unittest discover -s scripts -p test_*.py
docker compose config --quiet
git diff --check
git push --atomic origin main v0.9.6
gh run view <本次运行ID>
docker manifest inspect ghcr.io/h4ckbu7eer-sudo/lucentmist:0.9.6
```
