# LucentMist 0.9.8 发布清单

状态：0.9.8修正版发布产物已通过本任务验证；见[完整证据](release-artifact-validation-0.9.8.md)。0.9.7两项事实错误及[失败记录](release-artifact-validation-0.9.7.md)保留，不把旧CI成功当成本版语义通过。

- [x] 通用范围检查覆盖任意端口数字、共享服务目录、同目标范围合并和不同目标隔离
- [x] DNS不以一次响应比作为低风险阈值，默认呈现上限与目标无关
- [x] 版本同步到0.9.8，历史证据保留原版本
- [x] 证书年份/已扫端口误述修复，12个新增回归
- [x] 最终本地 build/test/format/Compose 全通过（590个.NET + 14个Python测试）
- [x] 精确授权key检查：工作树/暂存区/全部Git对象ABSENT（证据暂存后复查；提交后再次检查）
- [x] main与v0.9.8已推送，CI三job成功
- [x] GHCR manifest可读，原始镜像层校验下载/导入不可变digest；独立workflow直接pull并health200
- [x] 发布镜像真实网关分析4轮、0补查、DNS一致、历史线索折叠，147行
- [x] 同一镜像回环目标2轮、50行，明确是容器网络视角
- [x] 发布说明含完整命令和产物对话证据（0.9.7失败也保留）
- [x] 原有未提交lock文件364行依赖改动保留，不混入版本提交

部署前设置强凭据并备份明文扫描数据。云端线索非确认漏洞；未知、未检查不等于安全。
