# LucentMist v0.5.0 后续规划

> 状态：阶段 A-F 全部完成 ✅（v0.5.0 交付）
> 核实日期：2026-08-08
> 决策裁定：Sirius 加 gitignore ✅ / 框架维持现状 ✅（附注 Docker 缺 Web） / 范围先 A+B ✅
> v0.6.0 状态：本地前置修复完成（T4 单扫描入口 / T5 PoC 验证 / T6 CPE 统一 / Dockerfile / CI filter）；Docker build + CI push 待环境

## 1. 现状核实（以代码为准，不凭报告背书）

| 维度 | 实际状态 |
|------|----------|
| 分层架构 | Core -> Tools -> Agent -> CLI/API/Web，方向合理 |
| ReAct + 工具链 | 可运行；离线测试 132/132 通过 |
| ScanController | 已接真实 PingScanTool，但仍是 `Task.Run` + 静态字典，未用 BackgroundService/SQLite |
| AgentController | 仍是写死 SSE，未接真实 ReActEngine |
| ScannerActor | 仍是 Phase 3 占位实现 |
| Web | 页面直接调用本地 ScanService，未接 API 任务流/SignalR 进度 |
| Git | `.git` 目录存在但为空，不是有效 Git 仓库 |
| Docker | 构建阶段已使用 `sdk:10.0`，Web/tests 为 net10.0，主库为 net8.0 |
| CI | 已存在 ci.yml，测试步骤已无 `continue-on-error` |
| 仓库体积 | bin/obj 约 158 MB，publish 约 35 MB，Sirius 约 38 MB |
| 测试 | Core 13 + Agent 44 + Tools 75（非外网）= 132；Tools 完整 82 中另有 7 个外网 SSL 用例 |

## 2. v0.5.0 目标

把“能演示”变成“能交付”：

- API 扫描任务走真实后台队列，状态可查询。
- Agent SSE 输出真实 ReAct 事件。
- Web 页面展示真实任务结果，不再写死。
- Git 仓库有效、可发布，CI 真跑绿。
- 测试数字与实测一致。

## 3. 执行阶段

### A. API 真实链路（P0）✅ 已完成

- ✅ `ScanStore`（SQLite 持久化，`API/Persistence/ScanStore.cs`）
- ✅ `IScanCoordinator` + `ScanCoordinator`（Channel 队列）+ `ScanWorker`（BackgroundService）
- ✅ `ScanController` 真实任务：POST→202+taskId，GET 状态 pending→running→completed
- ✅ `AgentController` 接真实 ReActEngine，SSE 输出 thought/action/observation/message/error
- ✅ `ScannerActor` 通过注入的 `IScanner` 接口调真实扫描，不再空壳
- ✅ 额外修复：API 加 `RollForward=LatestMajor`（本机缺 8.0 ASP.NET 运行时）；请求模型改普通类（record 位置参数绑定 bug）；AgentController 补本机 IP 注入；CS8425 取消令牌用 `[EnumeratorCancellation]` 传播
- ✅ 验证：`dotnet build` 0 警告 0 错误；测试 146/146；API 端到端真实扫描+SSE 已实测

### B. Web 接真实数据（P0）✅ 已完成

- ✅ 新建 `LucentMist.Scanning` 共享层（ScanStore/ScanCoordinator/ScanWorker/进度发布接口），API 与 Web 共用同一队列，API 重复实现已删
- ✅ ScanWorker 按 ping/tcp/udp 真实分派，状态+结果写 SQLite，兼容旧库自动补 ports 列
- ✅ Web 的 Home/Scan/Devices 页改为提交后台任务 + SignalR 实时进度，完成事件轮询兜底防丢结果
- ✅ Web 端口改 5051（避免与 API 5050 冲突）；AppState 改 scoped
- ✅ 新增 `tests/LucentMist.Scanning.Tests`（6 用例：持久化/状态流转/分页/旧表迁移/入队）
- ✅ 验证：`dotnet build` 0 警告 0 错误；测试 152/152（Core 13 + Scanning 6 + Agent 44 + Tools 89）；Web 三页 + API 健康均 200，无残留进程

### C. 仓库卫生 + Git（P1）

- 初始化有效 Git 仓库。
- 补充 `.gitignore`：`publish/`、`Sirius/`、`*.db`、`logs/`、`data/`。
- 清理/外移构建产物，打 v0.4.1 tag。

### D. CI + 测试补齐（P1）

- 新增 API 集成测试（创建任务、状态流转、非法 target）。
- 修 CLI status 版本号读取。

### E. 误报可信度（P1）

- CLI 输出区分“候选/已验证”。
- confirmed 字段在 API/CLI/Web 统一展示。

### F. 文档对齐 + 复盘（P2）

- 统一测试数字。
- 更新版本规划。
- 写 v0.5.0 复盘。

## 4. 待拍板

### 4.1 Sirius 去留

- 建议：暂不移动，先把 `Sirius/` 加入 `.gitignore`，保留集成代码。
- 备选：移动到 `E:\Sirius` 独立部署，项目内只留集成客户端。

### 4.2 框架版本

- 建议：维持现状，主库 net8.0、Web/tests net10.0，Docker 构建用 SDK 10.0。
- 备选：全部改 net8.0，但需要同步调整 Web 的静态资源 API。

### 4.3 v0.5.0 范围

- 建议：先做 A+B，验收后再进入 C-F。
- 备选：A-F 一次做完。

## 5. v0.6.0 本地修复记录

### T4 单扫描入口

- 删除 `ScannerActor` 执行体和 `IScanner`，保留 `StartScan/ScanProgress/ScanCompleted/ScanFailed` 消息类型供未来调度复用。
- `ScanWorker` 是唯一后台扫描入口。

### T5 PoC 验证

- 新增 `IVulnerabilityVerifier`，`PocVerifier` 接入 `VulnerabilityScanTool`。
- 内置库 CVE 输出 `confirmed = poc.Exploitable`，附 `proof` 与 `verificationDetail`。
- 验证器抛异常时降级为候选，不中断扫描。
- 新增 4 个测试用例。

### T6 CPE 映射统一

- 新建 `CpeCatalog` 作为唯一 CPE 数据源。
- `CpeMatcher` 与 `ServiceCpeMapping` 都从 `CpeCatalog` 读取，删除双份字典。
- 新增 3 个一致性测试。

### Dockerfile + CI

- 运行时 `runtime:8.0` → `aspnet:10.0`。
- Dockerfile 增加 `NUGET_SOURCE` 构建参数 + 双源 NuGet.config + restore 补全重试，国内网络可用华为云镜像构建。
- 外网测试统一 `[Trait("Category","External")]`，CI/Docker 测试排除 External，避免 build 依赖外网。
- ✅ 实测：`docker build` 成功；容器 health 200；scan 202 + completed 真实结果。
- 待补充：Web 入镜像（容器内 NuGet 网络不稳定，镜像暂为 API-only）、CLI 入镜像（保留本机运行）。

## 6. v0.7.0 完成项

- Docker 镜像完整：API + Web 均进入 `aspnet:10.0` 镜像，compose 双服务，API 5050 / Web 5051 实测 200。
- 解决容器内 NuGet 下载中断：双源 NuGet.config + restore 重试，移除已删除依赖的离线缓存。
- Web 新增 `/scan/history` 扫描历史页，展示最近 50 条真实任务并支持展开详情。
- 文档对齐：部署运维手册、技术设计方案、API 接口设计、README、STATUS 统一到 v0.7.0。
- 版本号统一：API/Web/CLI/报表输出均改为 0.7.0。

## 7. v0.8.0 完成项

- 性能基准：新增 `tests/LucentMist.Benchmarks`（BenchmarkDotNet），实测 Ping/Port/UDP 共 9 个场景，报告写入 `docs/16-性能基准报告.md`。
- Agent 会话持久化：新增 `AgentSessionStore`（SQLite），API 支持 `GET /api/v1/agent/sessions` 与详情回放；CLI 支持 `agent --list` / `agent --resume {id}`。
- 错误诊断：Ollama 不可达时输出“Ollama 服务不可达，请确认已运行 ollama serve”；Ping 空结果附加子网/防火墙提示。
- CLI 与 API 统一读取 `LMIST_LLM_*` 环境变量。
- 测试：离线 173/173（新增 AgentSessionStore 3 用例）。

## 8. v0.9.0 完成项

- Docker 可复现：`NUGET_SOURCE` 构建参数 + restore 重试；移除 Akka.Analyzers 离线缓存残留；Dockerfile 恢复 CLI 发布；部署手册 1.2 重写并指向根 Dockerfile。
- Web Agent 对话页：新增 `/agent`，支持 SSE 流式渲染 thought/action/observation/message/error，会话历史下拉与回放，侧边栏入口。
- TFM 决策：采用保守方案 A，在 `docs/02-技术设计方案.md` 固化分层 TFM 策略；不升级纯库到 net10。
